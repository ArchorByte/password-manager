#include <string>

#ifndef UTILS_GENERATOR_HPP
#define UTILS_GENERATOR_HPP

std::string generate_character_chain
(
    const int &length,
    const bool &use_letters,
    const bool &use_digits,
    const bool &use_special_characters
);

#endif
